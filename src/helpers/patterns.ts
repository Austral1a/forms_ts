export const patterns = {
    name: /^[a-zA-Z]+$/,
    email: /^[a-zA-Z]+@[a-zA-Z]+\.[a-zA-Z]+$/,
    password: /\w{6,}/
};

export const patternNames = {
    name: 'name',
    email: 'email',
    password: 'password'
};
